name: login
description: Login Session with Shared Preferences
publish_to: 'none'
version: 1.1.0+2
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.8
  http: ^0.13.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
flutter:
  uses-material-design: true
